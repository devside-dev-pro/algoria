{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'runner: pas de build (tsx exécute le TS directement)'"
  },
  "deploy": {
    "startCommand": "npm run runner",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
